[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -jar backend/target/promptbetter-backend-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
